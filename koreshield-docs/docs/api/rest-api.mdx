---
title: REST API
description: Generated OpenAPI contract, authentication model, and endpoint reference for KoreShield APIs.
slug: /api/rest-api
---

# REST API

KoreShield's REST API contract is generated from the live FastAPI app.

- Interactive docs (Swagger UI): `GET /docs`
- ReDoc: `GET /redoc`
- OpenAPI JSON: `GET /openapi.json`

## Authentication Model

Protected endpoints require authentication via one of these methods:

1. `Authorization: Bearer <jwt>` — JWT issued via `/v1/management/login`
2. `X-API-Key: ks_...` — API key provisioned through the management API
3. `ks_access_token` httpOnly cookie — JWT set during login

JWT validation is strict:

- issuer (`iss`) is required and verified
- audience (`aud`) is required and verified
- expiry (`exp`) and issued-at (`iat`) are verified
- key policy enforces either `HS256` secret mode or `RS256` keypair mode

## Core Endpoint Groups

### Health & Monitoring

| Endpoint | Method | Auth | Description |
|---|---|---|---|
| `/health` | GET | None | Basic health check |
| `/health/providers` | GET | None | Per-provider health status |
| `/status` | GET | None | Full status with statistics and provider info |
| `/metrics` | GET | None | Prometheus-format metrics |

### Security Proxy

| Endpoint | Method | Auth | Description |
|---|---|---|---|
| `/v1/chat/completions` | POST | Required | OpenAI-compatible chat endpoint with security scanning |
| `/v1/rag/scan` | POST | Required | RAG context scanning for indirect injection |

Both proxy endpoints are rate-limited via SlowAPI (default: `60/minute`, configurable via `security.rate_limit` in config).

### Management

| Endpoint | Method | Auth | Description |
|---|---|---|---|
| `/v1/management/signup` | POST | None | Create account |
| `/v1/management/login` | POST | None | Login and receive JWT |
| `/v1/management/config` | GET | Required | View server config (dev environments only, values redacted) |
| `/v1/management/rules/*` | Various | Required | Manage custom detection rules |
| `/v1/management/alerts/*` | Various | Required | Manage alert rules and channels |

### RBAC

| Endpoint | Method | Auth | Description |
|---|---|---|---|
| `/v1/rbac/*` | Various | Required | Role and permission management |

### Teams

| Endpoint | Method | Auth | Description |
|---|---|---|---|
| `/v1/teams/*` | Various | Required | Team creation and membership management |

### Analytics & Reports

| Endpoint | Method | Auth | Description |
|---|---|---|---|
| `/v1/analytics/*` | Various | Required | Usage and security analytics |
| `/v1/reports/*` | Various | Required | Generate and download reports |

## WebSocket

`/ws/events` provides real-time event streaming for dashboards and monitoring.

Auth via:
- Header: `Authorization: Bearer <jwt>`
- Cookie: `ks_access_token`

:::note
WebSocket endpoints are not part of the OpenAPI document, as OpenAPI does not describe WebSocket handlers in this framework.
:::

## Rate Limiting

All protected proxy endpoints use rate limits. The default limit can be adjusted in your dashboard settings (hosted) or deployment configuration (self-hosted).
