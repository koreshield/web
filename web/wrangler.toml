# Cloudflare Pages build configuration
[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--prefix=. --legacy-peer-deps"

# Enable Node.js compatibility for Functions
compatibility_flags = ["nodejs_compat"]
compatibility_date = "2024-01-01"
