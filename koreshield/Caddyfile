api.koreshield.com {
    # Status JSON for the frontend
    handle /status.json {
        root * /www/status
        file_server
    }

    # API Proxy
    handle {
        reverse_proxy api:8000
    }
}
